#!/bin/sh

if swaymsg -t get_outputs | jq -e '.[] | select(.name == "HEADLESS-1" and .active == true)' > /dev/null 2>&1; then
    printf 'Monitor: HEADLESS-1\n'
else
    slurp -f 'Monitor: %o' -or
fi
