# Browser skill configuration

The following environment variables tune the browser at launch. Set them before running `scripts/setup.sh`.

## `BROWSER_LANDING_URL`

Initial page to open. Default: `https://duckduckgo.com`.
